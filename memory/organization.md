# Organization Memory

- Keep emb-agent lightweight by default; only expand orchestration when the current action materially benefits from it.
- Prefer explicit truth sources, explicit permission gates, and explicit recovery pointers over hidden runtime behavior.
